{
  "name": "eslint-config",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "eslint配置",
  "keywords": [
    "eslint-config"
  ],
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@actions/exec": "^1.1.0",
    "@changesets/cli": "^2.20.0",
    "eslint": "^8.7.0",
    "prettier": "^2.5.1",
    "zx": "^4.3.0"
  }
}
